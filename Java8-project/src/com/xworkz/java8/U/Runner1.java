package com.xworkz.java8.U;

public interface Runner1 {
    int i =10;   // we can create a blank var in interface bcz it do not have constructor to intialize the value.
    // so it cosidered as final variable bcz once we create a variable we cannot modify its value
    // interface does not have a state of an object
}
